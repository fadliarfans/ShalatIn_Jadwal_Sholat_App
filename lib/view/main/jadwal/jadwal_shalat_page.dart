import 'package:flutter/material.dart';

class JadwalShalatPage extends StatelessWidget {
  const JadwalShalatPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Jadwal Shalat"),
    );
  }
}
